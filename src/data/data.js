/**
 * data.js : Handles data for various files including the hosted files 
***/

const { port } = require('./data.json');
const host = require('./host.js');

/**
 * I dont know if this file will be necessary yet, as I cant simply
 * import all the JSON data into the server itself. The goal, I guess,
 * whas to have a class that instantiates this stuff
 */

module.exports = {
    
}