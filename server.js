/**
 * server.js : Entry file for server
***/

const express = require('express');
const { options } = require('./src/data/data.js');
const app = express();

app.get('/', function(req, res, err) {
    
    

});